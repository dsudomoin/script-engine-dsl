package io.github.dsudomoin.migration

/**
 * Маркер отказа на предусловии: миграцию забраковали до того, как она обработала хоть один
 * элемент, — и значит, целевая система не тронута.
 *
 * Отличать это от падения на середине приходится всегда: там состояние частичное и его надо
 * разбирать руками, здесь — просто чинить вход и запускать заново. Kora-runner превращает
 * такой отказ в код возврата `2` вместо `1`, но только если счётчик обработанных нулевой:
 * само по себе исключение не доказывает, что тело миграции ничего не успело сделать.
 *
 * Маркер, а не базовый класс, чтобы исключение сохраняло свой естественный супертип
 * ([CsvStructureException] остаётся `IllegalArgumentException`). Своё предусловие объявляется
 * так же:
 * ```
 * class InputMissing(message: String) : RuntimeException(message), MigrationPrecondition
 * ```
 */
interface MigrationPrecondition
